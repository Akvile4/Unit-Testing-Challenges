const add = (num1,num2) => {
    return num1 + num2
}

let myArray = ["Dan", "Rhys", "Rob"];

const multiply = (num1, num2) => {
    return num1 * num2
}

const mySeries = {
    elite: true,
    criminal: false,
    mindhunter: true,
    bigmouth: false
};

const food = (cuisine, meal) => {
    dishes = [
        "cuisine",
        "meal"
    ] 
};

module.exports = {
    add,
    myArray,
    multiply,
    mySeries,
    food
}

